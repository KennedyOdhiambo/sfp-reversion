[project]
name = "sfp-reversion"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kennedy Odhiambo", email = "x.johnkennedy.x@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
sfp-reversion = "sfp_reversion:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
